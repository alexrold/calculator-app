module.exports = function (api) {
  api.cache(true);
  return {
    presets: ['babel-preset-expo'],
  };
};
// babel.config.js neutralizado: eliminado por solicitud del usuario.
// Si necesitas volver a añadir configuración para aliases, dímelo y la reaplico.
